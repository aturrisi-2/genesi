name: PR Validation

on:
  pull_request:
    branches:
      - gold-faro-stable

jobs:
  pr-validation:
    name: PR Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Basic validation
        run: |
          echo "PR validation passed"
